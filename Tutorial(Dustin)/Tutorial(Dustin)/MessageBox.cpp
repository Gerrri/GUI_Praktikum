/*
#include <Windows.h>

int WINAPI WinMain(HINSTANCE hInstance, HINSTANCE hPrevInstance, LPSTR lpCmdLine, int nCmdShow) {
	
	// pointer auf Windows , Text in der Box, Überschrif, möglichkeiten, Typ der Box
	int result = MessageBox(nullptr, "Test Text hahaha!", "Test Message Box", MB_OKCANCEL | MB_ICONERROR);

	if (result == 1) {
		MessageBox(nullptr, "OK", "Test Box", MB_OK | MB_ICONINFORMATION);
	}
	else if (result == 2) {
		MessageBox(nullptr, "Cancel", "Test Box", MB_OK | MB_ICONINFORMATION);
	}

	return 0;
}

*/